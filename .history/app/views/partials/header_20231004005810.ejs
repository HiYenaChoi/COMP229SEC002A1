<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="style.css"/>
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="/@fortawesome/fontawesome-free/css/all.min.css"/>
    <style>
        /* Add background color to the navbar */
        .navbar {
            background-color: rgb(255, 255, 255);
        }

        /* Space out the navbar items and remove the bullet points */
        ul.navbar-nav {
            list-style: none;
            display: flex;
            gap: 20px; /* Adjust the spacing between navbar items */
            justify-content: flex-end; /* Align items to the right */
            font-size: 20px;
        }

        /*ul.navbar-nav li {
            No need for display: inline-block; or float: right;
        }*/

        a:link {
            text-decoration: none;
        }
        a:visited {
            text-decoration: none;
        }
        a:hover {
            text-decoration: none;
        }
        a:active {
            text-decoration: none;
        }
        html, body {
            height: 100%;
            margin: 0;
            margin-left: 20px;
            margin-right: 20px;
        }
        body {
            display: flex;
            flex-direction: column;
            padding-left: 200px;
            padding-right: 200px;
        }
        main {
            flex: 1;
        }
        footer {
            background-color: white;
            color: darkgoldenrod;
            padding: 10px;
        }
        footer .navbar-text {
            margin-bottom: 0;
        }

        .button-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    margin-top: 20px; /* 버튼과 welcome-message 사이의 간격 조정 (원하는 간격으로 조절) */
}

.redirect-button {
    padding: 10px 20px;
    font-size: 18px;
    border: none;
    cursor: pointer;
    height: 20%; /* 버튼의 너비를 100%로 설정하여 동일한 크기로 만듭니다. */
}


    </style>
</head>
<body>